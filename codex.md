앞으로 이 폴더에서 대화 시작 시 항상 이 codex.md 읽고 시작

이 문서의 정본은 github.com/eiqq/GCB-AI 리포지토리의 codex.md 다 (로컬 클론: 자비스\GCB-AI).
대화 시작 시 GCB-AI를 git pull 하고, repo의 codex.md가 이 파일과 다르면 repo 버전으로 이 파일(자비스\codex.md)을 스스로 업데이트하고 다시 읽는다.
반대로 세션 중 이 파일을 수정했으면 GCB-AI에도 복사해 커밋·푸시한다.

이 프로젝트는 "게임캐릭터배틀" 마인크래프트 서버이다
이 폴더 자체는 github repo가 아니다. 별도 폴더에 서버 소스파일들을 모아둔것.
대화 시작시 "별도 파일" 폴더가 없다면 만들어 둘것 이것은 사용자가 추가로 넣고싶은 파일들 지정하는것.

배경설명:
    기본적으로 히어로 슈팅 배틀로얄이다
    각 히어로마다 능력 4개가 있다.
    마인크래프트 자체 기능을 거의 쓰지 않고 자체적인 구현을 쓴다
    이 폴더의 "GCBAPI" 폴더 프로젝트는 이 서버의 핵심 구현 을 모두 담고 있는 플러그인 소스코드이다.
    "Personal" 폴더는 서버 실행 파일 그 자체이다.
    위 두개 폴더는 별도 github repo이다.

    "별도 파일" 폴더는 유저가 원본이랑 별개로 캐릭터 추가 시 사용할 리소스같은거 적당히 모두 넣는곳이다.
    "GCB-resourcepack" 폴더는 리소스팩 정본 github repo(github.com/eiqq/GCB-resourcepack)의 클론이다. 루트가 그대로 단일 리소스팩(pack.mcmeta+assets, 텍스처 veryHigh+사운드 병합)이며, 리소스팩 수정은 여기서 하고 커밋한다. zip은 커밋 금지. 플레이어 배포는 서버 플러그인 GCBResourcePackManager가 이 팩에서 품질별 zip을 생성해 GitHub Release(eiqq/GCB-resourcepack-dist)에 자동 업로드하고 config URL·SHA-1을 갱신하는 방식이다. 포크/창작 버전에서 자기 팩을 배포하는 법은 GCB-resourcepack README의 "배포" 항목을 따른다.
    main 브랜치는 본서버용만. 창작/개조버전 캐릭터 리소스는 main에 푸시하지 말고 자기 이름(또는 창작물 이름)의 branch를 파서 거기에 전부 넣는다.
    "resourcepack" 폴더는 서버 리소스팩 배포본 zip 보관용이다(레거시 참고용). 이펙트추가,무기추가,사운드추가,캐릭터 일러스트 수정의 자료구조 기준은 이제 GCB-resourcepack repo다.

    핵심 구현은 GCBAPI에 있지만, 실제 활용과 동작은 은 이 GCBAPI가 자체적으론 거의 하지 않고 서버 파일에 있는 "Skript" 플러그인이 한다. 캐릭터 코드, 게임진행 로직 대부분은 이 Skript로 GCBAPI기능을 가져와서 구현하는식으로 구현되어있는 구조이다.
    Skript 에드온 기능은 거의 사용 하진 않는다.(greather than 같은 문법을 > 로 바꾸는 에드온정도는 사용 중)

대화 시작시 유의사항:
    사용자에게 "실제 서버 폴더 경로" 를 묻는다.
    GCBAPI, Personal, GCB-resourcepack의 업데이트가 있는지 git으로 확인 한다.
    확인시 업데이트가 없으면 아무것도 안 물어도 되고, 있다면 사용자에게 위 업데이트가 있는데 다운로드 받을 지 말지 묻는다.
    리소스팩 정본은 GCB-resourcepack repo이므로 별도 다운로드는 불필요하다. 배포본(GitHub Release)과의 대조가 필요할 때만
    "\personal\파오캐\plugins\GCBResourcePackManager" config의 VERY_HIGH 와 SOUND 링크로 zip을 받아 비교한다.

    GCBAPI, Personal 은 원본으로 유지해야 한다. 말 그대로 참고용 백과사전 정도로 취급해야 한다.
    사용자에게 "실제 서버 폴더 경로" 를 묻고 모든 변경은 거기에서 하며. "별도 파일" 폴더에서 유저의 리소스나 참고자료를 찾을 수 있다.
    resourcepack 폴더 내의 원본 zip을 훼손하지 않는 선에서 resourcepack 내에서 zip파일 압축 해제 후 참고 해도 된다.

    GCBAPI 코드구조와 기능들을 학습한다. 

특정 캐릭터의 특징:
    캐릭터 41, 16 은 미완성이니 레퍼런스로 하면 안된다.
    32,68,70 은 '3인칭캐릭터' 이며 기존캐릭터와 차원이 다른 복잡함이니, 유저가 직접 "나는 3인칭 캐릭터를 제작하고싶어" 라고 말하는게 아니면 레퍼런스론 하지마라.


참고할 위치:
    "\personal\파오캐\코드 템플릿":
        이 경로에는 참고할만한 기초 코드 템플릿이 있다. 거의 최신문법으로 유지되나, 항상 그런것은 아닐수도 있기에 최 우선 고려사항은 GCBAPI내부의 로직을 우선 참고 하나, 차선으로는 이 문법을 참고.

    "\personal\파오캐\plugins\Skript\scripts":
        이 경로에 Skirpt 언어로 작성된 모든 구현이 있다.
    
    "\personal\파오캐\plugins\Skript\scripts\z":
        z 폴더 내에 있는 구현이 "캐릭터" 들 이며 캐릭터 자체, 각 스킬, 무기 등등은 여기 서 찾는다.
    
    "\personal\파오캐\plugins\Skript\scripts\a":
        a 폴더 내의 구현은 GCBAPI를 그대로 가져와 함수화 했거나 추가적으로 구현한 핵심엔진 폴더이다. 여기에 직접 구현 안 해놔도 GCBAPI에서 직접 임포트 해서 가져오긴 하나. 대부분은 a폴더에서 따로 스크립트 함수들로 구현후 사용 하고 있다.

    "\personal\파오캐\plugins\Skript\scripts\b":
        b폴더 내의 구현은 쉽게말해 '없어도되는' 즉 게임 진행이나 유틸기능을 구현한 엔진이다. 없어도 캐릭터의 작동에는 99% 문제없는 식이다.

    "\personal\파오캐\plugins\Skript\scripts\guis":
        gui 모음이다. 게임 시작 전 캐릭터 선택, 캐릭터 업그레이드, 특정 캐릭터의 커스터마이징 강화 등이 이것에서 이루어진다.
    
    "\personal\파오캐\plugins\Skript\scripts\y_augments":
        "증강" 모음이다. 캐릭터를 강화하는 '아이템' 정도로 이해하면 된다.

    "\personal\파오캐\plugins\Skript\scripts\z_weapon":
        "공용총기" 모음이다 'APEXWeaponSelector'스킬을 공유하는 캐릭터의 총기 는 여기 폴더에 대부분 구현되어있지만. 특정 캐릭터 전용 총기는 그 캐릭터 폴더(z폴더 내의) 에 구현되어있다

    "\personal\파오캐\plugins\Skript\scripts\z_npc":
        "인게임 non-player, interactable 엔티티" 모음이다. 

    나머지 폴더는 적당히 이해하면 되며, 캐릭터 추가에 핵심적이지 않다. a,z,y 폴더가 가장 핵심이며 "캐릭터 추가도움, 제작된 캐릭터 코드 검토" 등의 질문에는 이 GCBAPI와 a,z(다른 캐릭터와 코드 비교 포함),y 핵심폴더 위주로 검토 하면 된다.

리소스팩 수정 시:
    이펙트 추가 방식은 asset\minecraft\items 의 potion.json을 수정하는 방식이 아니다.
    예시로 하면, "\assets\gcb\items\effect\ahsin" 같거나 비슷한 식으로 추가 한다.
    ahsin은 캐릭터이름 영문 이다.
    이 방식은 "\assets\gcb\models\item\effect\ahsin" 폴더나 "assets\gcb\models\item\effect\ahsin" 에도 파일 추가 해야 한다.

    캐릭터 일러스트는 png는 "\assets\gcb\textures\item\icon" 에 추가하며. models 랑 items 에도 같이 추가 해야 한다.

    기계적으로 이 지시문을 따르지 말고 리소스팩 구조를 항상 검토 하여 깔끔한 구조를 가지도록 결론을 도출 하라.

    사용자에게 "수정된 리소스팩 결과물" 을 줘야 할 경우. output폴더 생성 후 거기에 수정된 리소스팩 파일을 내보내라.
    "수정된 리소스팩" 은 사운드의 경우 "추가된 사운드" 를 제외하곤 원본에 있던 사운드는 제거해서 출력(Sound.json 작성 포함)
    이펙트,일러스트,무기 의 경우도 마찬가지로 하라.

    즉 기본 리소스팩 + 추가 리소스팩 을 같이 장착하면 되도록 이 "추가 리소스팩" 은 원본에 없는 부분만 남기면 된다.

#--------------------------------------
모델 투명도(실시간 반투명) 플래그 — 26.3 리소스팩 전용:
    왜 필요한가:
        26.3 클라는 면(quad)마다 그 면 UV 영역의 텍스처 알파로 렌더 방식을 정한다. 텍스처 알파가 0/255 뿐이면 cutout 으로 그려져서
        스크립트로 투명도를 줘도(틴트 알파) "거의 0이면 안 보임 / 아니면 불투명" 으로만 동작하고 서서히 흐려지지 않는다.
        텍스처에 원래 반투명 픽셀이 있는 면은 자동으로 반투명이라 플래그가 필요 없다.

    켜는 법 (모델 JSON 의 textures 값을 문자열 → 객체로. 텍스처 png 는 건드리지 않음):
        켜기:  "textures": { "0": { "sprite": "modelengine:entity/models/ellen", "force_translucent": true } }
        끄기:  "textures": { "0": "modelengine:entity/models/ellen" }
        "particle": "#0" 같은 # 참조는 그대로 둔다.
        투명도는 틴트 알파로 주기 때문에 모델 면에 tintindex 가 있어야 하고, items 정의에 틴트가 있어야 한다.
            - GCB 이펙트(assets/gcb/items/effect/...)는 이미 포션 틴트(tints: minecraft:potion) + 전 면 tintindex 라 플래그만 켜면 된다.
              이펙트는 assets/gcb/models/item/effect/<캐릭터>/<이펙트>/<프레임>.json 프레임 파일 전부에 넣는다.
            - 코어 모델은 틴트가 없어서 전 면 tintindex 0 + textures 플래그 + items/core/<키>.json 포션 틴트(기본 흰색 16777215)를 같이 넣는다
              (_tools/install_cores.py 의 TRANSLUCENT 목록에 키를 넣고 설치하면 자동). 틴트용 복사 모델은 만들지 않는다.
            - ModelEngine 모델은 ME 가 팩을 다시 생성하면 원복되므로, 배포 팩은 GCBResourcePackManager config 의 force_translucent_models(모델 id 목록)에 넣는다.
              팩 생성 때 assets/modelengine/models/<id>/ 아래 JSON 전부(서브모델 포함)에 자동으로 넣어 준다.
              개발용 클라 폴더 팩에는 _tools/me_force_translucent.py <모델이름> (끄기 --off) 로 넣고, ME 동기화 뒤에는 다시 실행.
        적용 후 클라에서 F3+T.

    스크립트 사용법 (플래그와 짝):
        Effect_setOpacity(<이펙트 엔티티>, 0~1)         : ItemEffect(이펙트)·일반 ItemDisplay·createReal 분신 모두
        MEAPI_setOpacity(<엔티티>[, <모델id>], 0~1)     : 살아 있는 ModelEngine 모델
        다면체 이펙트(PolyEffect)는 별개 방식(CMD 알파 단계)이라 플래그가 필요 없다.

    주의:
        반투명 렌더는 비용이 조금 늘고, 알파 255 여도 뒤의 반투명 이펙트가 미세하게 비칠 수 있다.
        그래서 실시간 투명도 조절이 실제로 필요한 모델만 하나씩 켠다. 일괄 적용 금지.
        1.21.x 클라는 객체형 textures 를 못 읽으므로 1.21.x 팩에는 넣지 않는다.

캐릭터 추가,스켈레톤 생성 도움 요청시 지시문:
    일단 추가할 캐릭터의 이름과, 역할군을 묻는것으로 시작(역할군 정보를 토대로 기본 체력이나 공격력 스텟을 적당히 넣는다, 상세한것은 사용자가 할것)
    z폴더에 새 번호 폴더를 만드는것으로 시작
    새 캐릭터 번호는 900번 대 폴더는 무시하고 그 외에 가장 큰 번호로(사용자가 특별히 번호 지정한다면 그걸로, 상황이 애매하면 사용자에게 번호 묻기)

    유저에게 스킬 0 1 2 3 4 에 대해 묻는다:
        내용은 각 스킬은 어떤 스킬인가요? 어떤 효과를 가지나요? 구조는 뭔가요? 정도로 묻되, 유저가 "특정 캐릭터의 무슨 스킬을 레퍼런스로 해줘" 라고 하면 그렇게 하라.
        그럼 유저에게 할 질문은 "각 스킬은 어떤 스킬인가요?,구조는 뭔가요?, 어떤식으로 동작하나요? 아니면 특정 캐릭터의 스킬을 레퍼런스로 할까요?" 정도가 되겠네.

    10~30개이상의 캐릭터들을 검토 하여 캐릭터 코드 구조를 이해하는것부터 시작하라.

    나머지 번호 폴더들에서 레퍼런스를 따온다.

    우선순위는 git으로 체크해서 가장 최신의 변경사항이 있는 폴더 위주로 하되 그 변경사항이 '단순 수치 수정'이면 그건 무시하고 직접 비교하거나 commit 메시지등을 참고 해 본격적인 최신 업데이트 및 변경이 있는 캐릭터의 레퍼런스 위주로 검사.

    캐릭터 레퍼런스 생성시 코드 자체에는 일단은 필요한것만 넣어라. "on win:" 등은 필수가 아니다.
    각 레퍼런스에 WeaponAnimation_ 이나, Model_ 같은 함수들은, 1인칭 무기 모션과 스킬시전자의 3인칭 에니메이션관련이라 대부분의 경우 필수코드가 아닐것이다.
    레퍼런스는 항상 10개정도 검토하고, 필요하다고 판단된 부분, 혹은 많이 공통된 부분들을 필요한 부분으로 하여 작업한다.

캐릭터 코드 검토 요청, 특정 기능구현 도움 시 지시문:
    일단 다른캐릭터와 비교하는것부터 시작

    10~30개이상의 캐릭터들을 검토 하여 캐릭터 코드 구조를 이해하는것부터 시작하라.

    그 후  a폴더 , GCBAPI 순으로 기능 체크

    주의할점은 다른캐릭터의 함수나 이펙트 id를 복사해오는건 상관 없으니
    다른 캐릭터의 함수를 그대로 호출하는식의 구현은 안된다.

    완료 후 쓸모없는 import , 미사용 변수 , 미사용 option, 미사용 함수 제거 하되 사용자에게 한번 물어보고 하라.

    불필요한, 미도달 함수나 코드같은거 제거 잊지말되, 잘못 지우지 않도록 보수적으로 생각해서 도달가능성이 조금이라도 있으면 남겨라.

    유저가 쓰는 단어에서.
    "돌진" 은 대부분의 경우 이동하되 돌진중 중력은 없는거 말하는것일 가능성이 95%이상아며.
    "도약" 은 처음에 한번만 velocity적용하는, 중력도 있는, 점프같은것일 가능성이 높다.

Skript 주의사항:
    "on event:" 등에서 특정스킬의 cancel 이 다른 스킬에 영향을 주지 않도록 그 hasSkill이건 뭐건 빠른 종료로 내 스킬의 이벤트 수신이 다른 상관없는 이벤트에 영향을 안주게 주의. 이미 모든 코드가 이런 구조로 짜여 있음. 이걸 학습

    여러 if(가드)를 중첩할 때는 "가장 가벼운(계산 저렴한) 조건을 먼저" 배치할것. 무거운 조건(함수 호출, 변수/맵 조회, 문자열 포맷 등)은 뒤로 둔다. 특히 매 death/tick 마다 도는 핸들러에서 저렴한 타입 체크로 대다수 케이스를 먼저 걸러 무거운 평가를 아낄것. 예: on customafterdeath 에서 attacker is a player -> victim is a player -> Engine_has(...) 순.

#--------------------------------------
배경음악 추가 절차:
    3단계로 이루어진다:
        1. ogg 파일을 리소스팩 "assets/minecraft/sounds/music/" 에 넣는다 (파일명은 기존 스타일: 소문자, 붙여쓰기, 영문)
        2. "assets/minecraft/sounds.json" 에 한 줄 추가: "music.<이름>": {"sounds": [{"name": "music/<이름>","stream": true}]},
        3. "b/배경음악.sk" 의 on load 에 add "music.<이름>:<길이초>:<표시제목>" 추가
    길이초는 실제 ogg 길이와 일치해야 한다 (재생 종료 판정에 쓰이므로, 대충 넣으면 곡이 잘리거나 침묵이 생김).

    sounds.json 은 항목당 한 줄(탭 들여쓰기) 포맷을 유지 중이다.
    JSON 파서/에디터로 파싱→재직렬화 절대 금지 (파일 전체 포맷이 파괴됨). 항목 추가는 직접 문자열로 한 줄 삽입만 한다.
    삽입 위치는 일반 music 블록 맨 밑. 그 아래 특수 그룹(event/ending)과 파일 맨 끝 항목(쉼표 없음)은 건드리지 않는다.

    표시제목은 유명 영어 원제 OST 는 번역 없이 원제 그대로. "메탈커버" 같은 부연 설명은 붙이지 않는다.

#--------------------------------------
Skript 관용구 (직접 만들지 말고 팩의 기존 도구를 쓸 것):
    새 로직을 짜기 전에 비슷한 일을 하는 증강(y_augments)/캐릭터(z)/유틸(a) 파일을 먼저 grep 해서 기존 관용구를 따를 것. 자체 구현은 최후 수단.

    임시(지속시간 있는) 스탯/효과 = Buff() + onEnd(Runnable). 해제 로직은 onEnd 에 등록한다.
        옛 코드에 있는 "while Buff_hasBuff(...): wait 1 tick" 폴링은 구식이니 따라 쓰지 말 것 (버프마다 매 틱 태스크가 붙음).
    런타임 캐시/상태 = Collection_HashMap() (on load 에서 미설정 시 생성). Skript 전역변수를 캐시로 쓰지 말 것 — variables.csv 에 영구 직렬화되어 오염된다.
    경과시간/내부 쿨다운 = Counter_ (a/SimpleCounter.sk). Skript 는 변수로 timespan 을 못 만들기 때문에 ({_sec} seconds 는 컴파일 에러) 날짜 비교 방식은 인자 지속시간에 못 쓴다.

    스탯 보너스:
        addBonus 는 같은 id 로 다시 부르면 덮어쓰기가 아니라 누적된다. 중첩 효과에서 총합을 매번 다시 넣으면 폭주하니 증분만 더할 것. 값 교체는 setBonus.
        해제는 setBonus(id,0,op) 또는 StatModifier.remove(). Stats.clear() 는 스크립트에서 호출 금지 — 공격속도의 FAKE_AS(연출용 공속 상한 해제 모디파이어)까지 벗겨지는 함정이 있다.

    피해는 데미지()/데미지_미경직() 함수를 쓴다. "기본공격 피해 증폭" 류는 별도 데미지 인스턴스를 때리지 말고 이벤트에서 damage 값 자체를 수정할 것 (on customdamage 에서 skill contains "기본공격" 필터, 또는 on trybaseattack — 단 원거리 캐릭은 trybaseattack 시점에 대상을 모름).
    데미지 타입 문자열은 "물리"/"고정"/"자연" 뿐이다. "마법" 은 존재하지 않으며 넘기면 조용히 물리로 폴백된다 — 설명에 "마법 피해" 라고 쓰면 거짓말이 된다.

#--------------------------------------
아이템 이름/로어 작성 시 함정:
    스탯 색은 GCBAttribute 의 getColor() 가 권위이며 이미 legacy hex(§x§R§R... 형태)를 반환한다. <#RRGGBB> 태그로 감싸지 말 것 — 그 태그는 스크립트 파싱 시점 전용이라 런타임 조립 문자열에선 <> 가 화면에 그대로 보인다.
    스탯 한글명도 GCBAttribute 가 권위다. 예: ap=타격력(주문력 아님), pn=방어구관통력(관통력 아님).
    로어(설명)는 내부 구현이 아니라 플레이어가 보는 규칙으로 서술한다. 한 효과는 되도록 한 줄. 스타일은 y_augments 의 description placeholder 형식을 따를 것.
    Component 를 직접 조립하면 기본 italic 이 붙으니 꺼야 한다. Skript 의 set lore 경로는 알아서 꺼준다.

#--------------------------------------
이펙트 프레임 정합성:
    스킬 sk 의 effect.<이름>.frame: N 은 CustomModelData 1~N 을 순서대로 설정한다.
    대응 리소스팩은 텍스처/모델 <이름>/0 ~ N-1 (N장), items 의 range_dispatch entries 는 threshold 1~N-1 + fallback=0.
    entries 가 N-1 을 넘으면 마지막 프레임이 텍스처 없는 모델을 가리켜 깨진다.
    신규 이펙트 추가나 프레임 수 변경 시 이 3자(frame 값 / 텍스처 장수 / entries 최대값)의 정합성을 반드시 확인할 것.

#--------------------------------------
대화 종료시 대화 내용은 기억
모든 내용을 기억할필욘 없으나 유저의 스타일과 뭘 했었는지, 유저가 어떤 새로운 캐릭터를 제작했었는지, 유저가 알려준 서버폴더가 어디인지 정도만.